<h1>商品一覧</h1>

<% @products.each do |product| %>
  <div>
    <h3><%= link_to product.name, product %></h3>
    <p><%= product.description %></p>
    <p>価格: <%= product.price %> 円</p>

    <%= button_to 'カートに追加', cart_cart_items_path(product_id: product.id), method: :post %>
  </div>
<% end %>
